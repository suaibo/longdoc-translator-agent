# LongDoc Translator Agent 架构设计

版本：v0.3
状态：MVP 架构设计稿  
适用范围：论文翻译模式

---

## 1. 架构目标

LongDoc Translator Agent 的 MVP 目标是完成一个可演示、可恢复、可观察的长文档翻译 Agent 工作流。

架构设计重点：

- 长任务可分阶段执行。
- 术语确认作为 Human-in-the-Loop 中断点。
- 每个 chunk 翻译完成后写入检查点。
- 前端通过轮询查看任务状态和进度。
- 文档解析、Agent 编排、翻译、导出相互解耦。
- MVP 只支持单任务队列，避免并发复杂度。

---

## 2. 系统分层

```text
Gradio Blocks 工作台（挂载 /ui）
        |
        v
FastAPI API 层
        |
        v
应用服务层 / Domain Services
        |
        v
LangGraph Agent 工作流层
        |
        v
PostgreSQL + 文件存储 + OpenAI-compatible LLM + Docling
```

### 2.1 Gradio 工作台

职责：

- 上传文档。
- 展示任务列表和任务详情。
- 展示术语表确认页面。
- 展示翻译进度和 chunk 状态。
- 展示结果下载入口。

页面：

- 上传页
- 任务列表页
- 任务详情页
- 术语表确认页
- 翻译进度页
- 结果下载页

### 2.2 FastAPI API 层

职责：

- 暴露 REST API。
- 参数校验。
- 文件上传处理。
- 调用应用服务。
- 返回统一 JSON 响应。
- 对输出文件提供下载。

### 2.3 应用服务层

核心服务：

- `JobService`：创建任务、查询任务、状态流转、取消和恢复。
- `ParserService`：调用 Docling，将 PDF / MD / TXT 转换为统一 `ParsedBlock[]`。
- `DocumentIRService`：构建标题树、结构块和资产引用，持久化 DocumentIR Lite。
- `SemanticBoundaryService`：计算章节内候选语义边界，可无 embedding 回退。
- `ChunkService`：执行“结构边界优先、语义边界辅助、token 上限兜底”的混合切分；当前实现为规则基线。
- `TermService`：抽取术语、保存术语、处理用户确认译名。
- `TranslationService`：调用 LLM 翻译 chunk，维护术语和上下文。
- `LLMService`：按术语、翻译、摘要、质检任务路由模型；对可重试错误执行
  有限重试，并在主供应商耗尽后最多切换一次显式配置的备用供应商。
- `CheckpointService`：保存与读取检查点，支持失败续跑。
- `OutputService`：从 chunk 与 DocumentIR 生成 Markdown、报告及输出 manifest。
- `RenderService`：按 GFM / HTML / image fallback 渲染表格、公式和图片，并生成 HTML/结果包。
- `MetricService`：记录 token、耗时、失败次数、重试次数。
- `BudgetService`：在每次 LLM 调用前检查任务累计 token 与估算费用上限。
- `EventService`：持久化节点开始、完成、中断、失败和耗时事件。
- `ReviewService`：创建、查询和批准高风险 chunk/章节审核请求。

### 2.4 Agent 工作流层

使用 LangGraph 编排长任务节点。

工作流节点：

```text
parse_document
-> split_sections
-> extract_terms
-> interrupt_for_term_review
-> translate_chunk
-> summarize_chunk_context
-> mark_risks
-> generate_outputs
-> generate_report
```

说明：

- `interrupt_for_term_review` 是默认人工中断点。
- 可选启用 `interrupt_for_high_risk_review` 和
  `interrupt_for_chapter_review`；默认关闭，不影响基础闭环。
- `translate_chunk` 按 chunk 顺序执行。
- 每个 chunk 完成后写入数据库和检查点。
- 失败时任务进入 `FAILED`，保留已完成结果。
- `graph.py` 在节点外层统一埋点，业务节点不重复编写事件代码。
- 事件持久化失败只写服务日志，不能覆盖原节点异常。
- 质量检查出现 HIGH 问题时进入有限修订循环，次数由
  `MAX_REVISION_ATTEMPTS` 控制；每次修订与复检均记录独立指标。

### 2.5 持久化层

使用 PostgreSQL 保存结构化数据，使用本地文件系统保存上传文件、解析结果和导出结果。schema 由 Alembic 显式迁移；应用启动只检查连接，不自动建表。

---

## 3. 项目总目录结构

```text
longdoc-translator-agent/
├─ backend/
│  ├─ app/
│  │  ├─ api/
│  │  │  ├─ routes_health.py       健康检查
│  │  │  ├─ routes_jobs.py         上传、任务列表、详情和取消
│  │  │  ├─ routes_events.py       工作流事件时间线
│  │  │  ├─ routes_reviews.py      人工审核查询与批准
│  │  │  ├─ routes_terms.py        术语查询与确认
│  │  │  ├─ routes_chunks.py       chunk 进度查询
│  │  │  └─ routes_outputs.py      输出清单、文件与原文下载
│  │  ├─ core/
│  │  │  ├─ config.py              环境配置
│  │  │  ├─ errors.py              业务错误码与异常处理
│  │  │  ├─ response.py            统一响应体
│  │  │  └─ logging.py             日志配置
│  │  ├─ db/
│  │  │  ├─ base.py                SQLAlchemy Base
│  │  │  └─ session.py             PostgreSQL Engine、Session 与连接检查
│  │  ├─ models/
│  │  │  ├─ translation_job.py     翻译任务
│  │  │  ├─ document_chunk.py      文档分块
│  │  │  ├─ term_entry.py          术语条目
│  │  │  ├─ agent_checkpoint.py    Agent 检查点
│  │  │  ├─ translation_metric.py  翻译指标
│  │  │  ├─ workflow_event.py      工作流事件
│  │  │  ├─ review_request.py      人工审核请求
│  │  │  └─ risk_item.py           风险项
│  │  ├─ schemas/
│  │  │  ├─ common.py              通用响应 DTO
│  │  │  ├─ job.py                 任务 DTO
│  │  │  ├─ parser.py              ParsedBlock 与版面模型
│  │  │  ├─ chunk.py               ChunkDraft 中间模型
│  │  │  └─ document_ir.py         DocumentIR、SectionIR 与结构资产
│  │  ├─ services/
│  │  │  ├─ job_service.py         任务与上传服务
│  │  │  ├─ parser_service.py      PDF、Markdown、TXT 解析
│  │  │  ├─ layout_normalizer.py   双栏、页眉页脚和结构风险处理
│  │  │  ├─ document_ir_service.py DocumentIR 构建与持久化
│  │  │  ├─ semantic_boundary.py   语义边界评分
│  │  │  ├─ chunk_service.py       结构、语义和 token 混合切块
│  │  │  ├─ term_service.py        术语抽取与确认
│  │  │  ├─ translation_service.py 分块翻译与质量检查
│  │  │  ├─ checkpoint_service.py  检查点与恢复
│  │  │  ├─ budget_service.py      token 与费用预算
│  │  │  ├─ event_service.py       节点事件时间线
│  │  │  ├─ review_service.py      人工审核与恢复
│  │  │  ├─ output_service.py      输出文件与报告
│  │  │  └─ render_service.py      表格/公式/HTML/资源包渲染
│  │  ├─ agent/
│  │  │  ├─ state.py               LangGraph 状态
│  │  │  ├─ nodes.py               工作流节点
│  │  │  ├─ graph.py               Agent 图编排
│  │  │  ├─ runner.py              PostgreSQL checkpointer 运行入口
│  │  │  └─ prompts.py             Prompt 模板
│  │  ├─ storage/
│  │  │  └─ paths.py               本地存储路径约定
│  │  ├─ ui/
│  │  │  ├─ gradio_app.py          Gradio Blocks 与事件链
│  │  │  └─ handlers.py            事件级 Session 与 Service 调用
│  │  └─ main.py                   FastAPI 应用入口
│  ├─ alembic/                      PostgreSQL schema 迁移
│  ├─ tests/                       单元测试与集成测试
│  ├─ alembic.ini
│  └─ requirements.txt             Python 依赖
├─ docs/                            项目设计文档
├─ samples/                         演示与测试文档
├─ scripts/                         本地运行和检查脚本
├─ storage/
│  ├─ uploads/                      原始上传文件
│  ├─ parsed/                       标准化 Markdown
│  └─ outputs/                      译文与报告
├─ docker-compose.yml               PostgreSQL 开发库与测试库
├─ .env.example                     环境变量模板
├─ AGENTS.md                        项目上下文与开发约束
└─ README.md                        项目入口文档
```

目录规则：

- `api/` 只处理 HTTP 参数、响应和 service 调用，不承载复杂业务逻辑。
- `services/` 按任务、解析、切块、术语、翻译、检查点和输出拆分领域职责。
- `agent/` 只负责 LangGraph 状态与编排，LLM 调用通过 service 统一封装。
- `schemas/parser.py` 中的 `ParsedBlock` 是 PDF、Markdown、TXT 共用的解析中间模型。
- `storage/` 是运行时目录，不提交数据库、上传文件、解析结果和输出文件。
- 后续扩展继续遵循现有 api/service/agent/ui 边界。

---

## 4. 存储目录约定

```text
storage/
  uploads/{jobId}/
    original.ext
  parsed/{jobId}/
    document.md
    document.ir.json            # DocumentIR Lite
    assets/
      tables/
      formulas/
      figures/
  outputs/{jobId}/
    bilingual.md
    translated.md
    report.md
    bilingual.html              # 输出增强
    translated.html             # 输出增强
    manifest.json               # 输出增强
    result.zip                  # 输出增强
    assets/
```

规则：

- 上传文件保存到 `storage/uploads/{jobId}/`。
- 标准化 Markdown 保存为 `storage/parsed/{jobId}/document.md`，结构真相逐步迁移到 `document.ir.json`。
- 导出文件和相对资产保存到 `storage/outputs/{jobId}/`；最终使用 `result.zip` 保证 Markdown/HTML 与资产一起交付。
- 数据库只保存文件路径，不保存大段二进制文件。

---

## 5. 任务状态机

```text
UPLOADED
  -> PARSED
  -> WAITING_TERM_REVIEW
  -> TRANSLATING
  -> COMPLETED
```

异常状态：

```text
FAILED
CANCELLED
```

状态说明：

- `UPLOADED`：文件已上传，等待解析。
- `PARSED`：文档已解析并完成切分。
- `WAITING_TERM_REVIEW`：术语表已抽取，等待用户确认。
- `TRANSLATING`：正在分块翻译。
- `COMPLETED`：翻译完成，输出文件已生成。
- `FAILED`：任务失败，可尝试恢复。
- `CANCELLED`：任务取消，不再继续执行。

---

## 6. 核心工作流

### 6.1 创建任务

```text
前端上传文件
-> FastAPI 保存原始文件
-> 创建 translation_job
-> 后台 worker 开始执行 parse_document
```

创建成功后，前端拿到 `jobId` 并进入任务详情页。

### 6.2 解析与切分

```text
parse_document
-> Docling 解析 PDF / MD / TXT
-> Layout Normalizer 修正阅读顺序
-> 生成 ParsedBlock[] 与 document.md
-> 构建 document.ir.json、标题路径和结构资产
-> split_sections 执行结构/语义/token 混合切分
-> 写入 document_chunk
```

### 6.3 术语抽取与确认

```text
extract_terms
-> LLM 抽取术语和建议译名
-> 写入 term_entry
-> 任务状态变为 WAITING_TERM_REVIEW
-> 前端展示术语确认页
```

用户确认术语后：

```text
PUT /api/jobs/{jobId}/terms
-> 保存 confirmed_translation
-> 任务状态变为 TRANSLATING
-> 后台 worker 继续翻译
```

### 6.4 分块翻译

每个 chunk 翻译时携带：

- 用户确认术语表
- 当前章节标题
- 当前章节摘要
- 上一个 chunk 摘要
- 当前 chunk 原文

执行后：

- 写入 chunk 译文。
- 更新 chunk 状态。
- 写入 checkpoint。
- 更新任务进度。
- 记录 token 和耗时。

### 6.5 输出生成

所有 chunk 完成后：

```text
generate_outputs
-> bilingual.md / translated.md / report.md
-> bilingual.html / translated.html（输出增强）
-> manifest.json / result.zip（输出增强）
-> 任务状态变为 COMPLETED
```

---

## 7. 前后端交互逻辑

### 7.1 上传阶段

1. 前端调用 `POST /api/jobs` 上传文件。
2. 后端返回 `jobId`。
3. 前端跳转任务详情页。
4. 前端每 2 秒轮询 `GET /api/jobs/{jobId}`。

### 7.2 术语确认阶段

1. 任务状态变为 `WAITING_TERM_REVIEW`。
2. 前端调用 `GET /api/jobs/{jobId}/terms`。
3. 用户编辑术语译名。
4. 前端调用 `PUT /api/jobs/{jobId}/terms`。
5. 后端保存术语并继续翻译。

### 7.3 翻译阶段

1. 前端继续轮询任务详情。
2. 前端调用 `GET /api/jobs/{jobId}/chunks` 展示 chunk 进度。
3. 任务失败时展示错误和恢复按钮。
4. 用户点击恢复后调用 `POST /api/jobs/{jobId}/resume`。

### 7.4 完成阶段

1. 任务状态变为 `COMPLETED`。
2. 前端展示三个下载入口。
3. 用户分别下载双语译文、纯中文译文和报告。

---

## 8. 单任务队列策略

MVP 一次只允许一个任务处于运行中状态。

运行中状态包括：

- `UPLOADED`
- `PARSED`
- `WAITING_TERM_REVIEW`
- `TRANSLATING`

当存在运行中任务时，再次创建任务返回 `40902`。

---

## 9. 异常处理策略

### 9.1 Docling 解析失败

- 任务状态变为 `FAILED`。
- 错误码：`50002`。
- 保存错误信息。
- 不自动切换解析器。

### 9.2 LLM 调用失败

- 当前 chunk 标记为 `FAILED`。
- 任务状态变为 `FAILED`。
- 已完成 chunk 保留。
- 用户可调用 `resume` 继续。

### 9.3 用户取消任务

- 任务状态变为 `CANCELLED`。
- 后台 worker 检查到取消状态后停止后续 chunk。
- 已生成的中间结果保留。

---

## 10. 后续扩展点

- 多任务并发队列。
- 小说模式上下文记忆。
- 风险片段人工确认。
- 多解析器适配。
- DOCX、双语批注 PDF 和重排中文版 PDF。
- 用户系统和任务隔离。

---

## 11. Parser 与版面标准化

解析层统一输出 `ParsedBlock[]`，每个 block 至少保留：

- `blockId`、`kind`、`text`、`markdown`、`order`
- PDF 的 `pageNo` 和 bbox（含页面宽高）
- 标题级别、Docling 原始标签和版面调整元数据
- `TABLE / FORMULA / REFERENCE / STRUCTURE / LONG_PARAGRAPH / PARSER_WARNING` 风险

输入适配规则：

- PDF：固定使用 Docling `DocumentConverter`，OCR 支持 `auto / off / force`。
- Markdown：标题、段落、列表、围栏代码、块公式和 Markdown 表格转换为结构块。
- TXT：按空行形成段落块，随后进入相同标准化流程。

### 11.1 论文解析总原则

论文、硕博学位论文和技术报告不能只按纯文本顺序处理。Parser 必须先保留版面结构，再由 Layout Normalizer 修正阅读顺序，最后由 Chunk Service 决定翻译粒度。

固定原则：

- 标题、正文、列表、表格、公式、图片、caption、页眉和页脚先识别为独立结构块。
- 解析阶段不直接按字符数切割 PDF 文本，避免破坏栏顺序、表格行列和公式边界。
- 自动修正必须保留原始页码、bbox、Docling 标签和调整记录，方便报告追踪与人工复核。
- 无法可靠判断的内容优先保留原文并标记风险，不静默删除或猜测重建。
- Parser 负责生成和标准化 `ParsedBlock[]`；Chunk Service 负责在不破坏结构的前提下组合或拆分翻译 chunk。

### 11.2 双栏阅读顺序修正

论文常见的双栏页面在 PDF 文本流中可能出现“左一、右一、左二、右二”的交错顺序。标准化策略如下：

1. 按页聚合带 bbox 的 block。
2. 根据 block 宽度、横向中心和左右区域的纵向重叠判断是否存在双栏。
3. 同一双栏区域按“左栏从上到下，再右栏从上到下”重排。
4. 跨越大部分页面宽度的标题、摘要标题、表格、图片和公式视为分段边界，边界前后分别判断栏结构。
5. 不带 bbox 或栏归属不明确的 block 保留原始相对顺序。
6. 被自动重排的 block 写入 `layoutAdjusted=true`，并标记 `STRUCTURE` 风险。

目标不是还原原 PDF 的视觉排版，而是恢复适合连续阅读和翻译的语义顺序。

### 11.3 页眉页脚过滤

页眉、页脚、页码、学校名称、期刊名称和章节短标题可能在每页重复，不能进入术语抽取和正文翻译。

过滤分两层：

- 强规则：Docling 明确标记为 `page_header` 或 `page_footer` 的 block 直接从正文流移除。
- 重复规则：位于页面顶部或底部、规范化文本相同、且出现在至少两页或半数页面中的内容视为重复版面元素。

保护规则：

- 只按“出现页数”计数，不能因为同一页出现两次就判定为跨页重复。
- 正文区域的重复术语、章节标题和表格内容不能用该规则删除。
- 无法确定是正文还是页眉页脚时保留内容，并标记 `PARSER_WARNING`。

### 11.4 表格原子性

表格是结构块，不是若干普通文本行。Parser 阶段必须保持表格原子性：

- 一个小表格对应一个 `TABLE` block，保存原始 Markdown、caption、表头、数据行、页码和 bbox；增强阶段同时构建 TableIR 单元格结构。
- 不在 Parser 阶段把表格按行混入相邻正文，也不允许通用段落切分器从表格中间截断。
- 表格前后的 caption、注释和来源说明通过元数据与表格关联，不能被当作无关段落移动到其他章节。
- 合并单元格、跨页表格、列错位或识别置信度不足时保留 Docling 结果，并标记 `TABLE` 和 `PARSER_WARNING` 风险。
- MVP 保证结构尽量保留和风险可见，不承诺猜测性重建任意复杂表格。简单表格输出 GFM，复杂可信表格输出 HTML，低置信度表格输出原始截图和风险说明。

### 11.5 大表分组

当完整表格超过 Chunk Service 的 token 或行数阈值时，才允许按数据行分组。拆分规则如下：

1. caption、列头和表格注释先从数据行中独立识别。
2. 只在完整数据行之间切分，不拆单元格，不把一行切到两个 chunk。
3. 每个分组重复列头，使分组可以独立交给 LLM 理解和翻译。
4. 每个分组重复 caption；首组使用原 caption，后续组增加“续表”语义标记，例如 `Table 2 (continued)`，避免被误认为新表。
5. 表尾注释和来源说明默认放在最后一组；若注释约束所有分组，则通过上下文元数据注入每组，但输出时只渲染一次。
6. 所有分组共享 `tableGroupId`，并记录 `groupIndex`、`groupCount` 和原表 block ID。
7. 翻译完成后按分组顺序重新合并，输出阶段去除为了上下文而重复的 caption/header，只保留正确的连续表格结构。

小表默认整体翻译。只有大表确实超过阈值时才分组，不能为了追求统一而把所有表格机械拆散。

### 11.6 Caption 与 Header 重复处理

这里的重复分为两类，处理方式不同：

- 页面级重复：期刊页眉、论文短标题、页码等属于噪声，按页眉页脚规则过滤。
- 表格分组重复：caption 和列 header 是为了让大表分组保持上下文而主动复制，属于结构元数据，必须保留到翻译请求中。

实现约束：

- 主动复制的 caption/header 标记 `syntheticRepeat=true`，不作为新的术语抽取语料，不单独生成译文段落。
- 输出合并时根据 `tableGroupId` 去重；caption 只输出一次，列 header 按最终 Markdown 表格结构决定是否重复。
- 图 caption 与图片 block 保持邻接和关联；不跨章节移动，也不因文本相同而按页眉规则删除。
- caption 无法确定归属时保留原位置，并标记 `STRUCTURE` 风险。

### 11.7 公式与引用风险

公式和引用优先保持原样，不让通用翻译逻辑改写其结构。

公式处理：

- Docling `formula`、Markdown `$...$`、`$$...$$`、`\(...\)` 和 `\[...\]` 识别为公式内容。
- 行间公式作为独立 `FORMULA` block；行内公式保留在所属段落中。
- 公式符号、LaTeX、编号和交叉引用不翻译，只翻译公式周围的解释文本；低置信度时保留公式截图。
- 公式缺失、括号不闭合、编号错位或 OCR 后符号可疑时标记 `FORMULA` 风险。

引用处理：

- `[1]`、`[2-5]`、`(Smith, 2020)` 等引用标记保留原样。
- 参考文献条目可识别为 `REFERENCE` block，不能与正文段落交错重排。
- 引用密集、编号疑似丢失、作者年份格式异常或 OCR 混淆时标记 `REFERENCE` 风险。
- MVP 不自动校验引用是否真实存在，只检查解析后的结构完整性和明显异常。

### 11.8 OCR 与解析错误标记

OCR 是 PDF Parser 的解析选项，不作为独立业务主线：

- `auto`：优先使用 PDF 内置文本，需要时启用 OCR。
- `off`：关闭 OCR，适合文本型 PDF。
- `force`：整页 OCR，适合扫描版论文。

错误分级：

- 局部可恢复：乱码、异常空白、重复字符、低置信度文本、bbox 缺失、疑似漏字等保留原文，并在 block 上标记 `PARSER_WARNING`；若与版面顺序有关，同时标记 `STRUCTURE`。
- 结构可恢复：表格、公式或引用局部解析异常时保留对应 block，并叠加 `TABLE / FORMULA / REFERENCE` 风险。
- 文档不可恢复：PDF 损坏、加密无法读取、Docling pipeline 失败或无法生成有效 block 时，任务进入 `FAILED`，错误码使用 `50002`。

风险项至少记录：

- `jobId`、`blockId` 或后续生成的 `chunkId`
- 风险类型和严重级别
- 页码、bbox 和原文摘录
- 自动处理动作，例如“已重排双栏”或“已过滤重复页眉”
- 建议人工检查的信息

所有风险最终汇总到 `report.md`。自动修正不等于无风险，发生过结构调整的内容仍应可追踪。

### 11.9 Parser 与 Chunk Service 边界

Parser 输出结构化、已排序的 `ParsedBlock[]`，但不负责按照 LLM token 上限完成最终切块。

Chunk Service 必须遵守：

- 普通段落可按句边界拆分，不能截断单词、公式或引用标记。
- 标题与其后的正文保持章节归属。
- 小表、独立公式和短 caption 作为原子块处理。
- 大表只能按 11.5 的规则分组。
- block 上的风险和版面元数据必须传递到 chunk、数据库风险项和最终报告。

---

## 12. Chunk Service

Chunk Service 位于 Parser/DocumentIR 与后续 LangGraph/Translation Service 之间，输入为已排序的结构块，输出为可持久化的 `ChunkDraft[]` 和 `document_chunk` 记录。当前实现是结构感知规则基线，目标实现才是结构、语义与 token 的混合切分。

### 12.1 切块策略

- 当前：标题开启新的章节上下文，并与其后的正文保持相同 `sectionTitle`。目标：维护标题栈和完整 `sectionPath`。
- 普通段落按配置的 token 阈值合并；超长段落只在句子边界拆分。
- 行内公式和引用标记在句子拆分前使用占位保护，不能从中间断开。
- 独立公式、代码和图片作为原子 chunk，即使超过建议阈值也不机械截断。
- 小表和关联 caption 保持一个 `TABLE` chunk。
- 大表按完整数据行分组，每组重复表头；后续组 caption 增加 `(continued)`，并标记 `syntheticRepeat=true`。

配置项：

```text
CHUNK_MAX_TOKENS=1800               # 当前规则基线
CHUNK_TARGET_TOKENS=1200            # 目标
CHUNK_SOFT_MAX_TOKENS=1800          # 目标
CHUNK_HARD_MAX_TOKENS=2400          # 目标
TABLE_MAX_ROWS=20
```

当前 token 数为本地启发式估算：CJK 字符按一个 token 估算，其他非空白字符约每四个计一个 token。该数值用于切块预算，不作为真实模型计费数据。

### 12.2 结构元数据

`document_chunk` 除原文外保存：

- `chunkType`：`TEXT / TABLE / FORMULA / PICTURE / CODE`
- `sourceBlockIds`：来源 ParsedBlock ID 列表
- `structureMetadata`：页面、bbox、block 类型和表格分组信息
- `tokenEstimate`：本地 token 估算
- `hasRisk / riskSummary`：聚合后的风险信息

表格分组元数据包括：

```text
tableGroupId
groupIndex
groupCount
originalTableBlockId
caption
headerRows
syntheticRepeat
```

PDF caption 优先根据 Docling `caption_refs` 与 `self_ref` 关联；无法获得引用时，使用紧邻表格或图片的 caption 作为 fallback。

### 12.3 幂等与状态边界

- chunk ID 由 `jobId + chunkIndex` 稳定生成，重复执行不会新增重复记录。
- 只允许对 `UPLOADED / PARSED` 任务执行切块。
- 只允许重建 `PENDING` chunk；任何已进入翻译、完成、失败或跳过状态的 chunk 都拒绝覆盖。
- 重切后删除多余的旧 `PENDING` chunk，并重新生成对应风险项。
- 切块成功后任务进入 `PARSED`，`currentStage=split_sections`，并更新 chunk 总数。
- 整个 chunk、风险和任务状态更新在同一业务数据库事务中完成。

### 12.4 风险继承

ParsedBlock 风险去重后写入 chunk，并生成关联的 `risk_item`。风险元数据包含来源 block、页面、bbox 和表格分组信息，供后续报告与人工检查使用。

表格即使 Parser 未提前附加风险，Chunk Service 也会确保存在 `TABLE` 风险，避免绕过版面标准化时丢失提示。

### 12.5 当前限制

- Chunk Service 已由后台 Worker 和 LangGraph 自动调用。
- Docling 未提供 caption 引用且 caption 不相邻时，只能保留原顺序并等待风险检查。
- 跨页合并单元格和非 Markdown 规则表格不做猜测性重建。
- 数据库变更统一使用 Alembic；禁止继续增加应用启动时手写迁移。

---

## 13. DocumentIR Lite

DocumentIR Lite 是复杂论文结构的文件级中间表示，详细字段与决策见 `docs/文档结构与输出策略.md`。

职责边界：

- Parser 识别 block 和原始结构。
- Layout Normalizer 修正阅读顺序并保留调整记录。
- DocumentIR Service 建立标题树、`sectionPath`、TableIR、FormulaIR、FigureIR 和资产引用。
- Chunk Service 只决定翻译粒度，不承担复杂结构重建。
- Render Service 根据 IR 生成 Markdown、HTML、图片兜底和结果包。

DocumentIR 初期使用版本化 JSON 文件，不新增每个 block 一行的关系表。PostgreSQL 保存路径、版本和任务状态。

---

## 14. 混合切分目标架构

### 14.1 决策顺序

```text
硬结构边界
-> 章节内语义候选边界
-> target / soft / hard token 预算
-> 超长单句兜底
-> 小碎片回并
```

硬结构边界包括标题、表格、独立公式、图片/caption、代码、参考文献和附录。语义评分只能影响同一章节内普通正文，不能破坏硬边界。

语义评分器必须可插拔且可降级：

- 第一阶段使用词汇重合、术语连续性和话语标记。
- 后续可增加 embedding 余弦相似度。
- embedding 服务不可用时回退规则基线。
- 不为每个段落调用聊天模型。

每个 chunk 目标保存：

```text
sectionPath
boundaryReason
boundaryScore
semanticTopic（可选）
sourceBlockIds
tokenEstimate
```

当前代码已保存本节字段，并使用本地词汇相似度作为默认评分流程；embedding scorer 保持可插拔。

---

## 15. 复杂结构渲染与 PDF

### 15.1 表格

- 简单二维表格：GFM Markdown。
- 含合并单元格、多级表头但结构可信：HTML table。
- 低置信度、跨页关系不明或列错位：原始截图 + 可用 CSV/JSON + 风险说明。
- Markdown pipe 源码不承诺视觉对齐，不能把空格填充当作最终解决方案。

### 15.2 公式

- Markdown 保留 LaTeX。
- HTML 使用 KaTeX 或 MathJax。
- OCR/解析低置信度时附原始公式截图。

### 15.3 输出与 PDF

- Markdown 是可编辑交换格式。
- HTML 是用户直接阅读格式。
- result.zip 负责携带 Markdown、HTML、原始文件和所有相对资产。
- MVP 保留并下载原始 PDF，但不生成翻译 PDF。
- V1 优先实现原 PDF + 双语批注。
- V2 再实现基于 DocumentIR 的重排中文版 PDF。

### 15.4 渲染安全

- Render Service 对正文、caption、表格单元格和文件名执行 HTML escape。
- 仅输出标签白名单，禁止透传脚本、事件属性和 iframe。
- HTML 使用本地 CSS/公式资源，避免依赖未知远程脚本。
- result.zip 的所有路径必须规范化并限制在输出根目录内，禁止 `..`、绝对路径和盘符。
- manifest 和下载文件名使用同一套安全路径策略。
